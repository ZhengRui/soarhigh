import { VotingPage } from './VotingPage';

export const metadata = {
  title: 'Vote | Soar High Toastmasters',
  description: 'Cast your votes for the current Toastmasters meeting',
};

export default function VotePage() {
  return <VotingPage />;
}
